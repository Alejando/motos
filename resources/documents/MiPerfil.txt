Mi Perfil
url /mi-perfil

Método => UserController@profile

View => resources/client/profile.blade.php
	- $user // GlimGlam\Model\User
 
PHP
$User->acutions() // retorna las subastas en las que se ha participado
$user->totalWins() //todas las subastas ganadas
$user->totalAuctions() //total de subastas jugadas
$user->getUrlUpdate() //Retorna la url para actualzar los datos del usuario 
$user->saveAvatar($pathImg) //Guardar imagen
$user->favs(page)  


Angular
user.acutions(page) // retorna las subastas en las que participo 
user.totalWinds() // el total de subastas que ha ganado
user.save() //crea y/o actualiza los datos del usuario
user.saveAvatar(); //guarda/replaza el avatar
user->favs(page) // retorna las subastas marcadas como favoritas por el usuario

